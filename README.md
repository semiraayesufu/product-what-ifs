# Tab-Level Sharing

A prototype for a Google Docs feature idea: sharing a single document tab without exposing the rest of the doc. Tabs organize sections of one document, but sharing permissions currently apply to the whole file — this explores letting each tab carry its own access list and link, separate from the document-wide share settings.

Open `index.html` directly, or enable GitHub Pages on this repo to view it live.
